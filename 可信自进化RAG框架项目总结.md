# 基于可信度分层的自进化 RAG 框架

**项目设计总结文档**

**Knowledge Flywheel RAG / Trust-Aware Self-Evolving RAG**

**文档用途：** 项目方案总结、简历项目包装、系统设计说明  
**核心目标：** 让 RAG 系统在可控、安全、可审计的前提下越用越好

---

## 目录

1. [项目概述](#1-项目概述)
2. [背景与问题](#2-背景与问题)
3. [总体设计思想](#3-总体设计思想)
4. [三层可信知识池设计](#4-三层可信知识池设计)
5. [三个关键安全改进](#5-三个关键安全改进)
6. [知识飞轮闭环流程](#6-知识飞轮闭环流程)
7. [检索与权重融合策略](#7-检索与权重融合策略)
8. [核心模块划分](#8-核心模块划分)
9. [数据库表设计](#9-数据库表设计)
10. [技术栈建议](#10-技术栈建议)
11. [项目亮点与简历描述](#11-项目亮点与简历描述)
12. [总结](#12-总结)

---

## 1. 项目概述

本项目设计一个基于可信度分层的自进化 RAG 框架，目标是在用户真实问答过程中自动发现知识缺口、沉淀候选知识，并通过分层审核、权限隔离、可信度加权和生命周期管理机制，使知识库逐步变得更完整、更可靠。

与普通 RAG 系统相比，该项目不是只做“文档切分、向量化、检索、生成”的静态流程，而是进一步引入知识飞轮机制，让系统能够从每一次使用中积累可复用知识。

```text
用户提问
  ↓
RAG 检索与回答
  ↓
发现知识缺口
  ↓
沉淀低可信候选知识
  ↓
LLM 预审、来源验证、用户反馈、冲突检测
  ↓
进入中可信候选池
  ↓
人工终审
  ↓
进入高可信主知识库
  ↓
后续回答质量提升
```

项目的一句话概括是：**一个能够在真实问答过程中发现知识缺口，并通过可信度分层、权限隔离、LLM 预审、人工终审和生命周期管理机制持续进化的 RAG 框架。**

---

## 2. 背景与问题

传统 RAG 系统一般依赖人工提前维护知识库。典型流程是上传文档、切分 Chunk、生成 Embedding、写入向量数据库，然后在用户提问时进行检索增强生成。

这种方式在企业知识库、智能客服、技术问答、代码助手等场景中有明显价值，但也存在以下问题：

1. 知识库更新依赖人工维护，系统无法主动补齐缺失内容。
2. 用户真实问题暴露出的知识缺口难以及时沉淀。
3. 回答失败后，系统本身不会自动改进。
4. 如果直接让 LLM 自动写入知识库，容易把幻觉内容写进主库，造成知识污染。
5. 用户私有上下文、项目上下文和通用领域知识容易混在一起，带来权限与隐私风险。

因此，项目的核心不是让 LLM 无限制地“自我学习”，而是在可控的流程中完成知识补充、可信度晋升和人工最终确认。

---

## 3. 总体设计思想

本项目的核心设计思想是：**新知识不直接进入正式知识库，而是先进入低可信区域，再通过一套晋升规则逐步进入中可信池和高可信主库。**

知识在系统中的流动路径如下：

```text
低可信池（会话/用户/项目级临时知识）
        ↓
LLM 预审 + 来源验证 + 反馈评估 + 冲突检测
        ↓
中可信池（候选知识）
        ↓
人工审批
        ↓
高可信池（主知识库）
```

该设计本质上是“分级可信知识池 + 渐进式知识晋升机制 + 会话隔离记忆机制”。它既保留了系统自进化能力，又降低了自动学习带来的知识污染风险。

---

## 4. 三层可信知识池设计

### 4.1 高可信池：主知识库

高可信池是系统默认使用的主知识库，主要存放稳定、通用、经过验证的知识。它的检索权重最高，可以作为回答中的主要事实依据。

**主要来源：** 官方文档、企业内部知识库、人工整理文档、人工审批通过的知识、历史高质量问答。

**典型内容：** 产品说明、接口文档、业务流程规范、常见问题标准答案、经过验证的技术知识。

**使用策略：** 默认参与召回，优先级最高，回答时可以作为主要依据。

### 4.2 中可信池：候选知识库

中可信池存放已经通过初步审核，但还没有经过人工最终确认的候选知识。它的检索权重低于主知识库，但高于低可信池。

**主要来源：** 低可信池中被筛选出的知识、LLM 预审通过的内容、有明确来源证据但尚未人工确认的内容、多次被使用并获得正反馈的知识。

**使用策略：** 可以参与回答，但需要在 Prompt 中提醒模型该知识属于候选知识，使用时需结合高可信知识和上下文谨慎判断。

**晋升条件：** 需要综合来源可信度、用户反馈、使用次数、冲突检测结果、隐私风险和 LLM 质量评分。

### 4.3 低可信池：会话补充池

低可信池存放系统在使用过程中发现的临时知识，通常来自某次会话、某个用户、某个项目或一次工具查询结果。

**主要来源：** 用户在对话中补充的信息、当前会话发现的知识缺口、外部工具查询结果、用户纠错内容、LLM 对当前问题的临时总结。

**使用策略：** 默认只对当前会话、当前用户或当前项目开放，不直接进入全局知识库。

**安全策略：** 必须先经过隐私过滤、Scope 分类和必要的脱敏处理，才能进入公共候选流程。

| 知识池 | 可信度 | 检索权重 | 主要作用 | 是否可全局使用 |
|---|---|---|---|---|
| 高可信池 | 高 | 最高 | 正式知识库，作为主要事实依据 | 可以 |
| 中可信池 | 中 | 中等 | 候选知识，辅助回答和等待人工确认 | 有限使用 |
| 低可信池 | 低 | 最低 | 会话/用户/项目级临时知识 | 默认不可以 |

---

## 5. 三个关键安全改进

### 5.1 低可信池不默认跨用户开放

低可信池中可能包含个人信息、企业内部信息、项目代码、业务数据、数据库结构、调试日志、用户偏好和临时上下文。因此，低可信知识不能简单地对所有用户开放，即使检索权重很低也可能带来隐私泄露风险。

项目需要对每条低可信知识执行三步处理：

1. **隐私过滤：** 识别个人信息、敏感业务信息、密钥、账号、日志中的敏感字段等。
2. **Scope 分类：** 判断知识只属于当前会话、当前用户、当前项目、当前租户，还是具有公共候选价值。
3. **脱敏处理：** 对于可以进入公共候选流程的内容，去除用户标识、项目标识、账号信息和业务敏感字段。

| Scope 类型 | 含义 | 示例 |
|---|---|---|
| conversation | 仅当前会话可用 | 用户刚刚补充的临时问题背景 |
| user | 仅当前用户可用 | 用户长期偏好、学习方向、个人项目上下文 |
| project | 仅当前项目可用 | 某项目的技术栈、接口约定、部署方式 |
| tenant | 仅当前企业/租户可用 | 企业内部流程、内部知识库内容 |
| global_candidate | 可进入公共候选池 | 通用技术知识、公开文档总结 |
| global | 正式全局知识 | 人工审批后的主知识库内容 |

### 5.2 LLM 审批只作为预审

LLM 可以帮助系统做摘要、分类、格式化、去重、冲突初筛、敏感信息识别和质量评分，但不应作为事实正确性的唯一判断者。

因此，从低可信池进入中可信池时，LLM 的作用应该是“预审”，而不是“终审”。中可信池的晋升需要综合多个信号：

1. **来源可信度：** 知识是否来自官方文档、数据库、工具查询结果、用户确认或其他可靠来源。
2. **用户反馈：** 该知识相关回答是否被点赞、采纳、纠错或点踩。
3. **使用次数：** 该知识是否在多个相似问题中被重复使用。
4. **冲突检测：** 是否与高可信主知识库已有内容冲突。
5. **隐私风险：** 是否包含个人、项目或企业敏感信息。
6. **时效风险：** 该知识是否可能已经过期。

```text
promotion_score =
    LLM质量评分
  + 来源可信度
  + 用户正反馈
  + 使用次数
  - 冲突风险
  - 隐私风险
  - 过期风险
```

### 5.3 设计完整的知识生命周期

项目不能只关注知识如何进入知识库，还需要管理知识从生成、使用、晋升、降权、过期、回滚到拒绝的完整生命周期。

```text
生成
  ↓
入低可信池
  ↓
隐私过滤 / Scope 分类 / 去重 / 冲突检测
  ↓
LLM 预审
  ↓
进入中可信池
  ↓
人工审批
  ↓
进入高可信池
  ↓
持续监控
  ↓
过期 / 降权 / 回滚 / 删除 / 进入拒绝池
```

建议增加拒绝池，用于保存被判定为错误、过期、重复、敏感或无价值的知识。这样可以避免系统以后反复生成同样的错误知识，同时也便于审计和回滚。

---

## 6. 知识飞轮闭环流程

系统在一次问答中的完整闭环如下：

1. 用户输入问题。
2. Query Rewrite 对问题进行改写和补充。
3. Trust-Aware Retriever 按权限和可信度分层检索。
4. Hybrid Search 进行 BM25 + Vector 混合召回。
5. Rerank 对候选 Chunk 重新排序。
6. LLM 基于检索内容生成答案。
7. 质量评估模块判断是否存在低召回、低置信度、负反馈或知识缺口。
8. Knowledge Extractor 从会话、工具结果或用户纠错中抽取候选知识。
9. Privacy Filter 和 Scope Classifier 进行隐私过滤与作用域分类。
10. 写入低可信池。
11. 定期任务对低可信池进行去重、冲突检测、LLM 预审和来源验证。
12. 合格内容进入中可信池。
13. 管理员人工审批后进入高可信主知识库。

该流程使系统具备持续积累能力，但每一步都有安全控制，避免把不可靠内容直接沉淀为正式知识。

---

## 7. 检索与权重融合策略

检索时不建议简单地把所有知识混在一起做 TopK，而应采用“权限过滤 + 分层召回 + 加权排序”的策略。

```text
高可信池 TopK = 5
中可信池 TopK = 3
当前会话低可信池 TopK = 3
当前用户低可信池 TopK = 2
公共候选低可信池 TopK = 1
```

召回后可以根据语义相似度、可信度、时效性、反馈和作用域进行综合排序。

```text
final_score = semantic_score
            × trust_weight
            × freshness_weight
            × feedback_weight
            × scope_weight
```

| 知识类型 | 建议权重 | 说明 |
|---|---:|---|
| 高可信主知识 | 1.00 | 默认主依据，优先使用 |
| 中可信候选知识 | 0.70 | 可辅助回答，但需要谨慎 |
| 当前会话低可信知识 | 0.60 | 对当前对话价值较高 |
| 当前用户低可信知识 | 0.45 | 可辅助个性化回答 |
| 公共低可信候选知识 | 0.15 | 弱召回，只作探索 |
| 过期知识 | 0.05 或不参与 | 通常不建议进入回答上下文 |

---

## 8. 核心模块划分

| 模块 | 职责 |
|---|---|
| Query Rewrite 模块 | 对用户问题进行改写、补全和多查询生成，提高召回率。 |
| Hybrid Search 模块 | 结合 BM25 和向量检索，实现关键词匹配与语义匹配互补。 |
| Rerank 模块 | 对召回结果重新排序，提高上下文质量。 |
| Trust-Aware Retriever | 根据知识可信度、权限、作用域和权重进行分层检索。 |
| Answer Generator | 基于检索结果生成回答，并显式区分高可信与候选知识。 |
| Knowledge Gap Detector | 检测低召回、低置信度、用户负反馈和知识缺口。 |
| Knowledge Extractor | 从会话、用户纠错、工具结果中抽取可验证知识断言。 |
| Privacy Filter | 识别敏感信息，阻止私有知识进入公共流程。 |
| Scope Classifier | 判断知识属于 conversation、user、project、tenant 还是 global_candidate。 |
| Knowledge Promotion | 根据综合评分控制知识从低可信池晋升到中可信池。 |
| Conflict Detector | 检测新知识与主知识库是否冲突。 |
| Feedback Collector | 收集点赞、点踩、采纳、纠错和追问信号。 |
| Human Review | 为进入高可信池的知识提供人工终审。 |
| Lifecycle Manager | 管理过期、降权、回滚、拒绝池、重复检测和版本追踪。 |

---

## 9. 数据库表设计

工程实现上不一定要物理创建三个向量库，可以使用统一知识表，通过 `trust_level`、`scope_type`、`status` 等字段实现逻辑分层。这样更便于去重、迁移、过滤、版本管理和权限控制。

```sql
CREATE TABLE knowledge_item (
    id BIGSERIAL PRIMARY KEY,
    title VARCHAR(255),
    content TEXT NOT NULL,
    embedding vector(1536),

    trust_level VARCHAR(32) NOT NULL,
    status VARCHAR(32) NOT NULL,
    scope_type VARCHAR(32) NOT NULL,

    user_id VARCHAR(64),
    conversation_id VARCHAR(64),
    project_id VARCHAR(64),
    tenant_id VARCHAR(64),

    source_type VARCHAR(64),
    source_ref TEXT,
    evidence TEXT,

    llm_score NUMERIC(5, 4),
    source_score NUMERIC(5, 4),
    feedback_score NUMERIC(5, 4),
    conflict_score NUMERIC(5, 4),
    promotion_score NUMERIC(5, 4),

    usage_count INT DEFAULT 0,
    positive_feedback_count INT DEFAULT 0,
    negative_feedback_count INT DEFAULT 0,

    reject_reason TEXT,
    version INT DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    expires_at TIMESTAMP
);
```

如果使用 PostgreSQL + PGVector，可以直接在 `knowledge_item` 表中存储 `embedding` 字段。如果使用 Milvus，则可以将 metadata 保存在 PostgreSQL 中，将向量和主键保存在 Milvus 中。

---

## 10. 技术栈建议

| 层级 | 推荐技术 |
|---|---|
| 后端框架 | Spring Boot |
| AI 编排 | Spring AI |
| 向量数据库 | PGVector 或 Milvus |
| 结构化数据库 | PostgreSQL / MySQL |
| 检索方式 | BM25 + Vector Hybrid Search |
| 重排模型 | BGE-Reranker、Cohere Rerank 或本地 Rerank 模型 |
| LLM | OpenAI、DeepSeek、Qwen、Claude 等 |
| 任务调度 | Spring Scheduling、XXL-JOB |
| 消息队列 | RabbitMQ、Kafka |
| 管理后台 | Vue3 / React |

---

## 11. 项目亮点与简历描述

### 11.1 项目亮点

1. **自进化知识飞轮：** 系统能够从真实问答过程中发现知识缺口，并沉淀为候选知识。
2. **分层可信知识池：** 通过低可信池、中可信池和高可信池控制知识晋升过程，避免错误知识直接污染主库。
3. **权限与作用域隔离：** 通过 conversation、user、project、tenant、global 等 Scope 控制知识可见范围。
4. **LLM 预审而非 LLM 终审：** LLM 只负责初步筛选、摘要、分类和评分，最终晋升需要来源验证和人工确认。
5. **知识生命周期管理：** 支持过期、降权、回滚、拒绝池、重复检测、冲突处理和版本追踪。

### 11.2 简历项目描述

可以在简历中这样描述该项目：

> 设计并实现基于可信度分层的自进化 RAG 框架，通过高可信主知识库、中可信候选池、低可信会话池构建知识飞轮机制。系统在问答过程中自动检测低召回、低置信度和用户负反馈等知识缺口，抽取候选知识并进行隐私过滤、作用域分类和脱敏处理。引入 LLM 预审、来源校验、用户反馈、冲突检测和人工终审机制，实现知识从低可信池到高可信主库的渐进式晋升。同时设计知识生命周期管理能力，支持知识过期、降权、回滚、拒绝池、重复检测和版本追踪，降低自动学习过程中的知识污染风险，提升 RAG 系统的长期可用性和问答准确率。

---

## 12. 总结

该项目的核心价值在于：它不是简单实现一个 RAG 问答系统，而是在 RAG 的基础上引入可信度分层、权限隔离、知识晋升和生命周期管理，让系统具备可控的持续进化能力。

从工程角度看，该方案可以作为企业知识库、技术问答平台、智能客服、AgentFlow 平台和代码助手的底层能力。从简历角度看，它体现了对 RAG 检索、知识库治理、Agent 自动化、系统安全和工程可落地性的综合理解。

最终，该项目可以被定义为：**基于可信度分层的自进化 RAG 框架，即 Trust-Aware Self-Evolving RAG。**
